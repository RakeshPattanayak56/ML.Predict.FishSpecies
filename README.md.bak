# ML.Predict.FishSpecies

To Run this project:
Go to local python folder, for example - cd C:\Users\Rakesh.DC\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Python 3.9

Run the following commonad from the above path.
pip install --upgrade pip
pip install pandas
pip install sklearn
pip install Flask
=================================

1. Open the project in Visual Studio code.
2. Open ML_FishMarket and Run All(on Top of the open VS code tab).
3. Open app.py and Run(Top right corner in VS code).
